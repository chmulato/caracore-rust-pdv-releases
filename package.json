{
  "name": "caracore-pdv-releases-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Testes unitários do portal CaraCore PDV",
  "scripts": {
    "test": "jest --no-coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
